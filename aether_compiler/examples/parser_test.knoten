// Test: Sprint 180 Parser Verification Script
// Lexer must handle // comments robustly without parsing errors: even with colons

import "examples/imported_ast.nod";

// Nested block syntax within UI component arguments, followed by trailing semicolon
let window = UIWindow("main", "Parser Test Window", UIVBox {
    UILabel("Imported value: " + ToString(imported_var));
});;

Print("Verification script compiled and executed successfully.");
